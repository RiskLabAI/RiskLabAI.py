[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    "quantecon",
    "ta",
    "numpy",
    "pandas",
    "statsmodels",
    "scikit-learn",
    "scipy"
]
